# UAT follow-up: 11 September 2026

## Published

- Separate preview generation from checkout loading; selecting a previous sample remains available while another request runs.
- Late preview responses are archived without replacing an explicitly selected sample or the sample being purchased.
- Pause narration for new illustrated albums; keep historical audio delivery and Lumi/Atelier narration intact.
- Floating Lumi without the paper background; full character in sample progress.
- Printed-edition notices on product pages and the shared footer. Current purchases remain digital only.
- Public-route-only aggregate page views and browser visits counted once per tab per Bucharest calendar day.
- Stripe enabled with verified test secret version 2; SmartBill left disabled.

Cloud Build: `e64cf153-c781-4bf0-9769-cf2d88f83a56`.
Cloud Run main: `povestea-mea-magica-00120-kkt`.
Cloud Run domain: `povestea-mea-magica-domain-00077-582`, 100% traffic.
Image digest: `sha256:812df014af6eafec3b04b7385053abf79be5e3967c04617478cd7bac042ebb18`.

## Verification

- 101 automated tests passed; TypeScript and production build passed.
- Desktop 1440px and mobile 390px: purchase first/second sample sends the selected order ID, including while a second sample is being created. Verified locally and against the deployed frontend with intercepted generation/checkout requests.
- Recovery after failed sample, polling outage, back, reload, and allowance exhaustion passed on desktop/mobile.
- Lumi figure loaded with object-contain; launcher background transparent; no horizontal page overflow in these tests.
- Public health returned 200/ready; unauthenticated report POST returned 401.
- Stripe API confirmed test mode. No real payment or new AI-generated paid order was performed in this check.

## Daily report: activated and first send accepted

Approved recipient: office@povestea-mea-magica.ro.
Approved schedule: 09:00 Europe/Bucharest; previous calendar day in the same timezone.
No customer names, child details, email addresses, order tokens or private URL paths in report content.

Endpoint: POST /api/reports/daily, protected by existing worker OIDC identity.
Aggregates and immutable send payload stored in Firestore dailyReports/YYYY-MM-DD.
Resend idempotency key prevents duplicate sends during bounded retries.

The owner explicitly approved project-level roles/logging.viewer for the application service account; the role was granted on 11 September. Cloud Scheduler job pmm-daily-report is ENABLED, 0 9 * * *, Europe/Bucharest, authenticated with the existing worker OIDC identity. Four retries are configured within a six-hour maximum window.

The first attempt returned a logging 403 during IAM propagation. The second returned HTTP 200 at 20:18:47 UTC. Firestore dailyReports/2026-09-10 confirms sent=true to the approved recipient, meaning Resend accepted the email. The existing Resend key could not read delivery status (GET returned 401), so inbox delivery has not been independently confirmed. Next scheduled execution: 12 September 2026, 09:00 Bucharest.

Provisioning is reproducible with scripts/provision-daily-report.sh. No application redeployment was needed for this activation. Do not report email receipt based only on API acceptance.

Counting caveats: browser visits are not unique people; client events may be blocked or spoofed. Generation starts include retries. Completed covers are not completed three-page previews. Sales use server conversion events; test and unknown-mode payments are separate, and refunds are not netted. This is operational reporting, not accounting.
