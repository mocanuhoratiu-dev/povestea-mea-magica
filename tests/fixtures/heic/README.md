# Synthetic HEIC fixture

`quadrants.heic`: 1200 x 800 pixels, four solid color quadrants (red, green,
blue, yellow), created locally with Sharp and the macOS `sips` HEIC encoder.
Contains no photograph or personal information. Used to verify real decoding,
orientation, dimensions and conversion without contacting an AI service.
